name: Main Workflow
on:
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:        
    - name: Testing
      if: ${{ github.event.comment.body == 'ok to test'}}
      run: echo test
